from Message_Mod.Message_Obj import Message as Msg

message = Msg()
message.print_messages(
    "Simple Variation #######",
    "V(n, k) ######",
    "for test only, this string should be the longest so lets will try this ft."
)

# print(message.get_bigger_message())